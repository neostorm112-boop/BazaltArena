{
  "name": "basalt-arena",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "workspaces": [
    "client",
    "server",
    "bff",
    "admin"
  ],
  "scripts": {
    "setup": "sh -c 'test -f .env || cp .env.example .env' && sh -c 'test -f bff/.env || cp .env bff/.env' && npm install && npm run prisma:generate -w bff",
    "db:migrate": "npm run db:migrate:deploy -w bff && npm run db:normalize-roles",
    "db:normalize-roles": "bash deploy/run-user-role-normalize.sh",
    "db:seed": "npm run db:seed -w bff",
    "dev": "concurrently -n client,bff -c cyan,magenta \"npm run dev -w client\" \"npm run dev -w bff\"",
    "dev:all": "concurrently -n client,bff,admin -c cyan,magenta,green \"npm run dev -w client\" \"npm run dev -w bff\" \"npm run dev -w admin\"",
    "dev:mock": "concurrently -n client,server -c cyan,magenta \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build:all",
    "build:all": "npm run build -w bff && npm run build -w client && npm run build -w admin",
    "build:client": "npm run build -w client",
    "build:bff": "npm run build -w bff",
    "build:admin": "npm run build -w admin",
    "test": "npm run test:unit -w bff",
    "test:integration": "npm run test:integration -w bff",
    "start": "npm run start -w bff",
    "prepare": "husky",
    "format": "prettier . --write",
    "format:check": "prettier . --check",
    "lint": "eslint \"{bff,client,admin}/src/**/*.{ts,js,jsx}\"",
    "typecheck": "npm run typecheck -w bff",
    "verify": "npm run format:check && npm run lint && npm run typecheck && npm run test -w bff",
    "precommit:check": "lint-staged && npm run typecheck -w bff && npm run test:unit -w bff"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "concurrently": "^9.2.0",
    "eslint": "^9.39.4",
    "eslint-plugin-react-hooks": "^7.0.1",
    "eslint-plugin-react-refresh": "^0.5.2",
    "globals": "^17.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.6",
    "prettier": "^3.6.2",
    "typescript-eslint": "^8.46.0"
  },
  "lint-staged": {
    "{bff/src,client/src,admin/src}/**/*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "dependencies": {}
}
